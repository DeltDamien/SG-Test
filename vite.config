import { defineConfig } from 'vite';

export default defineConfig({
  base: '/SG-Test/',
  build: {
    outDir: 'dist',
  },
});

module.exports = {
    root: './',
    build: {
        outDir: 'dist',
    },
    publicDir: 'assets'
 } 