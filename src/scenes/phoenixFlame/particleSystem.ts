export interface particleSystemConfig = {
speed: number;
baseSize: number;
sizeVariation: number;
baseTimeToLive: number;
timeToLiveVariation: number;

};

export class ParticleSystem {

    constructor(par) {
    }
}